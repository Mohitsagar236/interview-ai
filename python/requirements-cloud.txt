# Cloud deployment requirements
# Excludes Windows-specific packages that won't work on Linux servers

# Core async and networking
asyncio
websockets>=12.0
aiofiles>=23.0.0

# Environment and configuration
python-dotenv>=1.0.0

# AI/LLM providers
openai>=1.0.0
anthropic>=0.18.0
groq>=0.4.0

# Transcription
deepgram-sdk>=3.0.0

# Document processing
pypdf>=3.0.0
python-docx>=0.8.11
Pillow>=10.0.0

# OCR (requires tesseract installed on server)
pytesseract>=0.3.10

# Vector embeddings and search
sentence-transformers>=2.2.0
chromadb>=0.4.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Additional utilities
requests>=2.31.0
httpx>=0.24.0
pydantic>=2.0.0

# Monitoring and logging (optional but recommended)
sentry-sdk>=1.40.0

# Note: Install tesseract-ocr on the server
# For Render: add to render.yaml build command
# For Railway: add to nixpacks.toml
# For Ubuntu: apt-get install tesseract-ocr
