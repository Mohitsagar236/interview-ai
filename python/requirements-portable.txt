# Portable / minimal requirements — all core features work with these.
# Heavy optional deps (torch, sentence-transformers, faster-whisper) are omitted
# so the first-run install is fast (<2 min on a normal connection).
# Users supply their own API key; transcription uses Deepgram (cloud) by default.

websockets==12.0
python-dotenv==1.0.1
numpy==1.26.4
pillow==10.4.0
pypdf==4.3.1
python-docx==1.1.2
httpx[http2]==0.27.2
requests>=2.31.0
urllib3>=2.0.0
soundfile==0.12.1
mss==9.0.1

# OCR (PaddleOCR is the primary engine; pytesseract is the fallback)
paddlepaddle>=2.6.0
paddleocr>=2.7.0
onnxruntime>=1.17.0
pytesseract==0.3.13

# Real-time transcription (cloud — requires Deepgram API key)
deepgram-sdk==3.2.7

# Windows native screen capture (for restricted apps like Teams)
pywin32>=306; sys_platform == 'win32'
