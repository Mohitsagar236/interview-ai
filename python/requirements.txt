# Full developer requirements.
# Production desktop packaging uses requirements-portable.txt.

websockets==12.0
python-dotenv==1.0.1
numpy==1.26.4
pillow==10.4.0
pypdf==4.3.1
python-docx==1.1.2
httpx[http2]==0.27.2
requests>=2.31.0
urllib3>=2.0.0
mss==9.0.1
soundfile==0.12.1

# OCR
paddlepaddle>=2.6.0
paddleocr>=2.7.0
pytesseract==0.3.13
onnxruntime==1.19.0

# Real-time streaming transcription
deepgram-sdk==3.2.7

# Windows screen capture
pywin32>=306; sys_platform == 'win32'
dxcam>=0.0.5; sys_platform == 'win32'

# Optional local speech-to-text and semantic search for development.
faster-whisper==1.0.3
sentence-transformers==3.0.1
faiss-cpu==1.8.0.post1

# Optional local LLM client for development.
ollama>=0.1.7

# Optional GPU/local-ML backends are intentionally not installed by default.
# torch>=2.0.0
# transformers>=4.36.0
# accelerate>=0.21.0
