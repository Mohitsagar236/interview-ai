# Legacy convenience file for local development from the repository root.
# Production desktop packaging uses python/requirements-portable.txt.
#
# Heavy local-ML packages such as torch, transformers, accelerate,
# bitsandbytes, faster-whisper, sentence-transformers, and faiss are
# intentionally not installed by default. They increase installer size and can
# trigger missing CUDA DLL warnings on machines without NVIDIA CUDA.

-r python/requirements-portable.txt
